{
  "name": "fixify-fullstack",
  "version": "1.0.0",
  "description": "FIXIFY - Complete Home Services Platform",
  "main": "BACKEND/server.js",
  "scripts": {
    "build": "cd Frontend && npm ci && npm run build",
    "start": "cd BACKEND && npm ci && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}